<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Без INTERNET приложение не сделает ни одного запроса к API.
         Flutter объявляет это разрешение только в debug- и profile-манифестах
         (см. android/app/src/{debug,profile}/AndroidManifest.xml), чтобы
         работали hot reload и отладчик; в релиз оно попадает только отсюда.
         Ни один из подключённых плагинов его не приносит — проверено по их
         манифестам. Отсутствие здесь не видно ни в отладке, ни в тестах:
         release-сборка молча остаётся без сети. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Геолокация нужна кнопке «Построить маршрут» (BuildRouteSection):
         стартом маршрута в Яндекс.Картах становится текущее место водителя,
         иначе первый отрезок пути — «где я сейчас → первая точка» — выпадает.
         Замер делается по нажатию кнопки, а не в фоне, поэтому WHEN_IN_USE
         достаточно и ACCESS_BACKGROUND_LOCATION не нужен.

         Это не то же самое, что ProductConfig.captureDeliveryCoordinates:
         тот флаг про запись координат точки на сервер, и он выключен, потому
         что полей latitude/longitude в API нет. Функции независимы — раньше
         разрешение убрали заодно с флагом, и построение маршрута от места
         водителя молча перестало работать на обеих платформах.

         COARSE объявлен рядом с FINE: на Android 12+ пользователь вправе
         выдать только приблизительное местоположение, и для точки старта
         маршрута этого хватает. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="CRM Millwater"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Звонок заказчику из карточки. Без этого на Android 11+
             url_launcher не видит приложение-телефон и тихо ничего не делает. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Построение маршрута в Яндекс.Картах. Без этих объявлений
             на Android 11+ canLaunchUrl не видит ни приложение карт,
             ни браузер и всегда возвращает false. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexmaps"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https" android:host="yandex.ru"/>
        </intent>
    </queries>
</manifest>
